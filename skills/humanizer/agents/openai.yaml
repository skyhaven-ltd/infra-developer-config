interface:
  display_name: "Humanizer"
  short_description: "Make writing sound natural and human-written."
  default_prompt: "Use $humanizer to make this text sound natural and human-written."

policy:
  allow_implicit_invocation: false
