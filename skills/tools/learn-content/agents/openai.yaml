interface:
  display_name: "Learn Content"
  short_description: "Turn sources into study notes, quizzes, and Obsidian learning notes."
  default_prompt: "Use $learn-content to study these URLs or notes, synthesize durable learning material, and optionally save the result as Markdown in my Obsidian vault."
policy:
  allow_implicit_invocation: true
