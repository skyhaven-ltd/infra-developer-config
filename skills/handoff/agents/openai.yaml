interface:
  display_name: "Handoff"
  short_description: "Write a compact handoff for the next session."
  default_prompt: "Use $handoff to create a handoff document for the next session."

policy:
  allow_implicit_invocation: false
