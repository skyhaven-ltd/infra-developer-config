<Window x:Class="CloudContext.App.ConnectionEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add connection" Height="390" Width="590" ResizeMode="NoResize" Icon="Assets/cloud-connect.ico"
        WindowStartupLocation="CenterOwner" Background="#F4F6F8">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="155" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <TextBlock x:Name="HeadingText" Grid.Row="0" Grid.ColumnSpan="2" Text="Add a connection" FontSize="23" FontWeight="SemiBold" Margin="0,0,0,18" />
    <TextBlock Grid.Row="1" Text="Connection type" VerticalAlignment="Center" />
    <ComboBox x:Name="KindBox" Grid.Row="1" Grid.Column="1" Margin="0,5" SelectionChanged="KindBox_SelectionChanged" />
    <TextBlock x:Name="TargetLabel" Grid.Row="2" Text="Target" VerticalAlignment="Center" />
    <TextBox x:Name="TargetBox" Grid.Row="2" Grid.Column="1" Margin="0,5" />

    <StackPanel x:Name="GitHubFields" Grid.Row="3" Grid.ColumnSpan="2" Visibility="Collapsed" Margin="0,8,0,0">
      <Grid x:Name="HostField">
        <Grid.ColumnDefinitions><ColumnDefinition Width="155" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
        <TextBlock Text="GitHub host" VerticalAlignment="Center" />
        <TextBox x:Name="HostBox" Grid.Column="1" Margin="0,5" />
      </Grid>
      <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="155" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
        <TextBlock Text="Expected GitHub user" VerticalAlignment="Center" />
        <TextBox x:Name="UserBox" Grid.Column="1" Margin="0,5" />
      </Grid>
    </StackPanel>

    <Border Grid.Row="4" Grid.ColumnSpan="2" Background="#E8EDF1" CornerRadius="3" Padding="10" Margin="0,16,0,0" VerticalAlignment="Top">
      <TextBlock x:Name="HelpText" Foreground="#344B5E" />
    </Border>

    <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
      <Button Content="Cancel" IsCancel="True" />
      <Button x:Name="SaveButton" Content="Add" IsDefault="True" Click="Save_Click" />
    </StackPanel>
  </Grid>
</Window>
