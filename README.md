# ops-developer-config

Portable developer configuration for Claude Code, Codex CLI, VS Code, Git, and shared automation skills. It stores tool settings, hooks, scripts, and reusable skills in one repository so a linked workstation can keep its developer environment consistent with version control.
